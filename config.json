{
  "api_key": "copy-your-gemini-api-key-here",
  "api_url": "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent",
  "default_format":"md",
  "model_type": "gemini",
  "model_name": "",
  "default_style": "minimal",
  "language": "en",
  "styles": {
    "minimal": {
      "en": "Briefly identify critical logic bugs. Use bullet points.",
      "fr": "Identifie brièvement les bugs logiques critiques. Utilise des listes à puces."
    },
    "detailed": {
      "en": "Perform a deep architectural and logic review. Suggest improvements for readability and performance.",
      "fr": "Effectue une revue approfondie de l'architecture et de la logique. Suggère des améliorations de lisibilité et de performance."
    },
    "security": {
      "en": "Focus exclusively on security vulnerabilities: memory leaks, buffer overflows, and race conditions.",
      "fr": "Concentre-toi exclusivement sur les failles de sécurité : fuites mémoire, overflows et race conditions."
    },
    "performance": {
      "en": "Analyze time and space complexity. Suggest optimizations to reduce CPU and memory usage.",
      "fr": "Analyse la complexité temporelle et spatiale. Suggère des optimisations pour réduire l'usage CPU et mémoire."
	 },
	 "audit": {
  	  "en": "You are a security auditor. Analyze this code and identify: memory leaks (missing free/delete, lost pointers), buffer overflows (unchecked bounds, unsafe string ops), null/dangling pointer dereferences, use-after-free, integer overflows, race conditions, and undefined behavior. For each issue found, provide: the exact line or pattern, the impact (crash/exploit/leak), and a concrete test case or input to trigger it (e.g. command, payload, edge case value).",
      "fr": "Tu es un auditeur sécurité. Analyse ce code et identifie : fuites mémoire (free/delete manquants, pointeurs perdus), buffer overflows (bornes non vérifiées, opérations unsafe sur strings), déréférencements nuls/dangling, use-after-free, integer overflows, race conditions, et comportements indéfinis. Pour chaque problème, fournis : la ligne ou le pattern exact, l'impact (crash/exploit/fuite), et un test concret pour le déclencher (commande, payload, valeur limite)."
     },
	"pedago": {
      "en": "You are a senior C++ developer. You receive an audit report listing bugs and vulnerabilities. For each issue mentioned in the report, provide: a clear explanation of why it is dangerous, the corrected code snippet (minimal and focused), and if relevant, the test that should now pass. Do not re-analyze the code — only address what the report explicitly flags.",
      "fr": "Tu es un développeur C++ senior. Tu reçois un rapport d'audit listant des bugs et vulnérabilités. Pour chaque problème mentionné dans le rapport, fournis : une explication claire de pourquoi c'est dangereux, le snippet de code corrigé (minimal et ciblé), et si pertinent, le test qui doit maintenant passer. Ne ré-analyse pas le code — traite uniquement ce que le rapport signale explicitement."
     }
   }
}
